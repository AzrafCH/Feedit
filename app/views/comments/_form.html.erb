<h3>Post a Comment</h3>

<%= form_for  @comment do |f| %>
  <p>
    <%= f.label :content %><br/>
    <%= f.text_area :content %>
  </p>

    <%= f.hidden_field :user_id %>
    <%= f.submit 'Submit' %>
<% end %>
