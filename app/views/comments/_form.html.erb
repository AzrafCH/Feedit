<h3>Post a Comment</h3>

<%= form_for [@post, @comment] do |f| %>

<%= f.collection_select :post_id, @post, :id, :title %><br>
  <p>
    <%= f.label :content %><br/>
    <%= f.text_area :content %>
  </p>

  <p>
    <%= f.hidden_field :user_id %>
    <%= f.submit 'Submit' %>
  </p>
<% end %>
