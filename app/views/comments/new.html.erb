<h1>leave a comment</h1>

<%= form_for @comment do |f| %>

  <%= f.label "Content: " %><br>
  <%= f.text_area :content %><br>

  <%= f.label :post_id, "Post" %><br>
  <%= f.collection_select :post_id, @posts, :id, :title %><br>

  <%= f.submit "Submit" %>
<% end %>
